<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hd.epac.dao.UserDao">
  <resultMap id="BaseResultMap" type="com.hd.epac.entity.User">
    <id column="userid" jdbcType="INTEGER" property="userid" />
    <result column="username" jdbcType="VARCHAR" property="username" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="registtime" jdbcType="TIMESTAMP" property="registtime" />
    <result column="place" jdbcType="VARCHAR" property="place" />
    <result column="age" jdbcType="INTEGER" property="age" />
    <result column="birth" jdbcType="VARCHAR" property="birth" />
    <result column="admin" jdbcType="INTEGER" property="admin" />
    <result column="state" jdbcType="INTEGER" property="state" />
    <result column="sex" jdbcType="INTEGER" property="sex" />
  </resultMap>
  <sql id="Base_Column_List">
    userid, username, password, registtime, place, age, birth, admin, state
  </sql>
    <select id="checkLogin" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from hd_user
        where username = #{username,jdbcType=VARCHAR} and password = #{password,jdbcType=VARCHAR}
    </select>

    <select id="findAllUsers" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from hd_user
    </select>

    <insert id="addUser">
    insert into hd_user (userid, username, password,
      registtime, place, age,
      birth, admin, state,
      sex)
    values (#{userid,jdbcType=INTEGER}, #{username,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR},
      #{registtime,jdbcType=TIMESTAMP}, #{place,jdbcType=VARCHAR}, #{age,jdbcType=INTEGER},
      #{birth,jdbcType=VARCHAR}, #{admin,jdbcType=INTEGER}, #{state,jdbcType=INTEGER},
      #{sex,jdbcType=INTEGER})
  </insert>
</mapper>